org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Скачивание с repo.maven.apache.org рвётся на рукопожатии, когда Gradle
# открывает соединения десятками сразу: тот же файл и curl, и Java по одному
# берут без единой ошибки. Два воркера без параллельных проектов проходят.
org.gradle.parallel=false
org.gradle.workers.max=2
